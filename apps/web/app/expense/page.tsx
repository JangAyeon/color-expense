import ExpenseManager from "../../@component/expense/expenseManager";

const ExpensePage = () => {
  return (
    <div>
      <h1>Expense Page</h1>
      <p>This is the expense page content.</p>
      <ExpenseManager />
    </div>
  );
};

export default ExpensePage;
